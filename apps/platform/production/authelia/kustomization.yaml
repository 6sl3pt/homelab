apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: authelia
resources:
  - ../../base/authelia
  - ../../../common/cloudflared/
  - secrets.yaml
  - users.yaml
  - cloudflare.yaml
  - referencegrant.yaml

configMapGenerator:
  - name: authelia-values
    files:
      - values.yaml=values.yaml
