apiVersion: v1
kind: Service
metadata:
  name: rackula-ui
spec:
  ports:
    - port: 8080
  selector:
    app: rackula
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: rackula-api
spec:
  ports:
    - port: 3001
  selector:
    app: rackula
  type: ClusterIP
