apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: r2-creds
spec:
  refreshInterval: "0"
  secretStoreRef:
    name: azure-kv-store
    kind: ClusterSecretStore
  data:
    - secretKey: kasm-r2-access-key-id
      remoteRef:
        key: kasm-r2-access-key-id
    - secretKey: kasm-r2-secret-access-key
      remoteRef:
        key: kasm-r2-secret-access-key
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kasm-db-creds
  namespace: kasm-workspaces
spec:
  refreshInterval: "0"
  secretStoreRef:
    name: azure-kv-store
    kind: ClusterSecretStore
  target:
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: kasm-db-username
    - secretKey: password
      remoteRef:
        key: kasm-db-password
