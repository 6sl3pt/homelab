apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: kasm-db-production
spec:
  immediate: true
  schedule: "0 0 3 * * *" # Go cron package format, back up at 3AM
  backupOwnerReference: cluster
  cluster:
    name: kasm-db-production-cnpg-v1
