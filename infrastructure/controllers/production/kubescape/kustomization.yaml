apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubescape
resources:
  - ../../base/kubescape

configMapGenerator:
  - name: kubescape-values
    files:
      - values.yaml=values.yaml
