apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: postfix
resources:
  - ../../base/postfix
  - secrets.yaml

configMapGenerator:
  - name: postfix-values
    files:
      - values.yaml=values.yaml
